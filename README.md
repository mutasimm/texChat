Peer to peer socket connection based chat app that support tex-like math formatting.

Note: if you are connecting as the server, you may need to configure your router for appropriate port forwarding.
